name: My First Workflow
on: [push]  # Triggers on code pushes
jobs:
  build:
    runs-on: ubuntu-latest  # Use a GitHub-hosted Linux runner
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run a script
        run: echo "Hello, GitHub Actions!"
